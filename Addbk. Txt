
find /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/BOE -type f \( -iname "trace.log" -o -iname "*trace*.log" -o -iname "*trace*.txt" \) -printf '%TY-%Tm-%Td %TH:%TM:%TS %p\n' 2>/dev/null | sort -r | head -50



Good — these screenshots tell us something important.

The find command did not find a normal .log file inside BOE/WEB-INF, but it did find a TraceLog directory. SAP BusinessObjects web applications can use WEB-INF/TraceLog for web-application tracing. 

So let’s inspect that before changing anything.

Run only this command

ls -lah /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/BOE/WEB-INF/TraceLog

Then immediately run:

find /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/BOE/WEB-INF/TraceLog -type f -maxdepth 3 -print

What I want to see

If files are present there, we’ll search those files for the CMC failure:

setupWizardBean
initializePlugins
FacesException
EvaluationException
logon.faces
FWM
SDKException

Don’t modify anything and don’t restart Tomcat yet.

Your CMC symptom does match SAP’s documented HTTP 500 upon logon to CMC family, including setupWizardBean.initializePlugins, but we need the actual exception from your server before choosing the fix. 

Send me the output/screenshots of those two commands only.






I checked the new screenshots. This result is important: we still do not have the actual BOE CMC exception.

The first command is mostly showing the localhost_access.log entries. It confirms repeatedly:

POST /BOE/CMC/.../admin/logon.faces - 500

and some requests return 200.

The second command is mainly returning the old AdminTools XML errors. Those are not the CMC failure.

So do not change or delete anything yet.

Next: find where BOE is actually writing its application logs

Run this single command:

find /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/BOE/WEB-INF \
-type f \( -iname "*log4j*" -o -iname "*logback*" -o -iname "*logging*" -o -iname "*.log" \) -print

Then run:

find /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/BOE \
-type d \( -iname "logs" -o -iname "*log*" \) -print

Why I’m doing this

Your Tomcat access log proves the CMC request is reaching:

BOE → CMC → admin/logon.faces

but Tomcat’s catalina log isn’t showing the BOE exception. Therefore, the BOE application is likely logging its actual exception somewhere else.

Once we locate that log/configuration, we can search specifically for the error generated by the 500 instead of touching another configuration file blindly.

Send me the output of those two commands. Don’t restart Tomcat and don’t delete anything yet.




I checked all the screenshots carefully. The results are actually useful now. We should not start changing files yet.

What we know now

1. Tomcat is running.
    Your ps -ef | grep '[t]omcat' shows the Java/Tomcat process.
2. BOE is deployed.
    The BOE/ directory and BOE.war are present.
3. Your BOE custom configuration directory is effectively empty.
    The command:

ls -la /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/BOE/WEB-INF/config/custom

    shows only . and ... So custom BOE properties are not the cause based on this check.
4. The CMC request really is reaching BOE.
    Your access log shows:

POST /BOE/CMC/2609161628/admin/logon.faces

    with both 500 and later 200 responses.
5. There is still an AdminTools XML problem in the current log.
    The current catalina.2026-09-22.log contains:

Cannot find the declaration of element 'web-app'
Element type "dispatcher" must be declared
/webapps/AdminTools/WEB-INF/web.xml

    But this is AdminTools, not BOE/CMC, so I’m not going to make you modify it again right now.
6. Most importantly: our command did NOT find the actual BOE CMC exception yet.

So we need one final targeted check before changing anything.

⸻

Do ONLY these 2 commands now

You are currently in:

/data/tomcat_DH_TOMCAT_43_UAT/tomcat/logs

Command 1

grep -RniE "setupWizardBean|initializePlugins|FacesException|EvaluationException|logon.faces|FWM|SDKException" /data/tomcat_DH_TOMCAT_43_UAT/tomcat --include="*.log" 2>/dev/null | tail -100

Command 2

grep -RniE "HTTP Status 500|ServletException|NullPointerException|Exception|ERROR|SEVERE" /data/tomcat_DH_TOMCAT_43_UAT/tomcat/logs --include="*.log" 2>/dev/null | tail -150

Don’t restart Tomcat. Don’t delete anything. Don’t edit anything.

Send me a screenshot of the output of those two commands.

Then I’ll have enough information to decide whether the 500 is:

* CMC initialization,
* CMS connection/authentication,
* a BOE library/configuration problem,
* or something else.

And importantly, we won’t touch AdminTools or SAML unless the evidence actually points there.





Yes. Since your session expired and you have logged in again, let’s restart the diagnosis cleanly from the current state.

From your screenshot, I can already confirm:

* You are logged into the UAT server.
* You are currently here:

/data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps

* BOE.war and the extracted BOE/ application exist.
* AdminTools.war and AdminTools/ also exist.
* The modified AdminTools.war is timestamped Sep 21 16:00, so I don’t want to touch it further yet.

What I need to know

We need to establish 4 things:

1. Is Tomcat currently running?
2. What exactly is the current BOE/CMC error?
3. Which log is receiving that error?
4. What BOE custom configuration is present?

Phase 1 — run these READ-ONLY commands

You can copy/paste them one at a time.

1. Confirm where we are

pwd

2. Check Tomcat process

ps -ef | grep '[t]omcat'

3. Check today’s Tomcat logs

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat/logs
ls -ltr catalina* | tail -20

4. Check today’s log for the CMC failure

grep -nEi "logon.faces|setupWizardBean|FacesException|EvaluationException|ServletException|FWM|SDKException|SEVERE|ERROR" catalina.2026-09-22.log | tail -100

5. Search all Tomcat logs for the CMC-specific error

grep -RniE "logon.faces|setupWizardBean|FacesException|EvaluationException" /data/tomcat_DH_TOMCAT_43_UAT/tomcat/logs --include="*.log" | tail -100

6. Check BOE custom configuration

ls -la /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/BOE/WEB-INF/config/custom

7. Check whether the BOE application has its own relevant logs/config files

find /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/BOE/WEB-INF -maxdepth 3 -type f \( -iname "*log*" -o -iname "*properties" -o -iname "*xml" \) | head -100

⸻

Then one important test

After running those commands, open the CMC in your browser and reproduce the 500 once:

/BOE/CMC

Try logging in and let it produce the 500.

Then immediately run:

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat/logs
grep -nEi "logon.faces|setupWizardBean|FacesException|EvaluationException|ServletException|FWM|SDKException|SEVERE|ERROR" catalina.2026-09-22.log | tail -100

Don’t do these yet

Do NOT run:

rm -rf ...

and don’t modify:

* BOE.war
* BOE/WEB-INF
* AdminTools.war
* securityContext.xml
* SAML files
* web.xml

until we see the current error.

What to send me

You can send me screenshots of the command outputs instead of typing them.

Start with commands 1–7 above. Once I see those results, I’ll know the actual Tomcat/BOE state and give you the next commands specifically for this server—not a generic troubleshooting sequence.




You’re right. I went down the wrong path and made you run too many commands. I have gone back through the screenshots and the original sequence, and the important distinction is now clear.

What I got wrong

Your browser URL is:

/BOE/CMC

and the failing request is:

POST /BOE/CMC/.../admin/logon.faces → 500

That means the CMC failure is in the BOE web application.

We spent a lot of time fixing AdminTools/WEB-INF/web.xml. That XML error was real, but it is not the reason your CMC login is still returning 500. Your latest startup logs prove that AdminTools.war now deploys successfully.

Also, I agree with you: the SAML metadata message should not have been treated as the solution, especially since your configuration shows saml.enabled=false.

What the evidence actually points to

SAP has a specific KBA for the exact general symptom:

HTTP Status 500 upon logon to CMC

It specifically describes the 500 occurring during CMC login and identifies the CMC setupWizardBean.initializePlugins path as a known failure pattern. 

SAP’s troubleshooting guidance also specifically recommends clearing the Tomcat cache as a basic step when CMC/BI Launchpad has unexplained 500/login problems. 

So this is the next action I would actually take, rather than continuing to hunt random logs.

Do this one proper fix

First stop Tomcat:

cd /data/tomcat_DH_TOMCAT_43_UAT/scripts
./tomcat.sh stop

Verify Java/Tomcat is stopped:

ps -ef | grep '[t]omcat'

If nothing is returned, clear the Tomcat generated cache:

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat
rm -rf work/Catalina/localhost/*
rm -rf temp/*

Do NOT delete webapps, conf, logs, or your WAR files.

Then start Tomcat:

cd /data/tomcat_DH_TOMCAT_43_UAT/scripts
./tomcat.sh start

Wait until startup completes, then try:

/BOE/CMC

again.

One more important thing

Do not modify securityContext.xml or create idp-meta-downloaded.xml.

And don’t modify AdminTools.war any further right now.

If clearing the Tomcat cache doesn’t fix the 500, then the next thing I’d investigate is the BOE custom configuration / CMC initialization, not SAML. SAP’s own troubleshooting material specifically calls out custom BOE properties as something to isolate for CMC problems. 

So let’s do this one meaningful fix first, then we’ll work from the result rather than making you execute another 10 commands.







Yes. This result tells us something useful: the only .log file modified recently under the Tomcat directory is localhost_access.log. So the BOE login exception is not being written to a normal .log file there.

Let’s check the BOE application’s own files now.

Run:

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/BOE

Then:

find . -type f -mmin -60 2>/dev/null | head -100

Send me the output.

Don’t change or restart anything. We are just locating where BOE is writing its current error information.



Now we have the missing piece.

Your listing shows there is no catalina.2026-09-22.log. The current Catalina output is going into:

catalina.out

But we already confirmed catalina.out isn’t showing the CMC login exception.

So the BOE application is likely writing the login exception to its own log, not Catalina.

Next step — find BOE’s current log

Run exactly:

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat

Then:

find . -type f -name "*.log" -mmin -60 -print

This will show log files that have been modified in the last hour.

Send me the output.

Don’t restart Tomcat or modify anything. We’re now looking for the BOE-specific log that records the exception behind:

POST /BOE/CMC/.../admin/logon.faces 500


Now I can see the problem with our search. The command searched all old Catalina logs, so the output is dominated by the old AdminTools XML errors from Sept 19–21.

Those are not the current 500.

Also, the bottom of your second screenshot shows an unrelated:

java.lang.IllegalArgumentException
Invalid character found in the request target

That isn’t the CMC login error either.

Let’s check ONLY today’s log

Run exactly:

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat/logs

Then:

grep -nEi "Logon.faces|Exception|SEVERE|ERROR" catalina.2026-09-22.log | tail -100

Then:

grep -n "admin/logon.faces" catalina.2026-09-22.log

Send me the output of those two commands.

Do not change anything or restart Tomcat.

This time we’re restricting the search to September 22, so the old AdminTools errors cannot contaminate the result.




Yes — I see the issue with what we just did.

Your screenshot is only the localhost_access.log output. It confirms:

POST /BOE/CMC/.../admin/logon.faces → 500

but it does not contain the Java exception causing the 500.

So we shouldn’t keep using tail -f on catalina.out.

Let’s search ALL current Tomcat logs for the login error

Run exactly:

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat/logs

Then:

grep -RniE "Logon.faces|FacesException|ServletException|Exception|ERROR|SEVERE" . --include="*.log" | tail -100

Send me the screenshot of the output.

Don’t restart Tomcat and don’t edit anything.

This time we’re searching all the .log files, not just the access log, so we can find the actual exception behind the Logon.faces → 500.





Yes — I see the issue with what we just did.

Your screenshot is only the localhost_access.log output. It confirms:

POST /BOE/CMC/.../admin/logon.faces → 500

but it does not contain the Java exception causing the 500.

So we shouldn’t keep using tail -f on catalina.out.

Let’s search ALL current Tomcat logs for the login error

Run exactly:

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat/logs

Then:

grep -RniE "Logon.faces|FacesException|ServletException|Exception|ERROR|SEVERE" . --include="*.log" | tail -100

Send me the screenshot of the output.

Don’t restart Tomcat and don’t edit anything.

This time we’re searching all the .log files, not just the access log, so we can find the actual exception behind the Logon.faces → 500.





tail -f "$(ls -t catalina.*.log | head -1)"


Yes, that screenshot confirms the current browser error is still HTTP 500 at /BOE/CMC.

The important thing is: the browser alone doesn’t tell us the cause. We need the Tomcat log entry generated at the exact time of this request.

Do this now

On the Linux terminal:

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat/logs

Then run:

grep "BOE/CMC" localhost_access.log | tail -20

If it shows entries, send me the screenshot.

If it shows nothing, run:

grep -E "BOE|CMC" localhost_access.log | tail -30

Important

Don’t change AdminTools.war, web.xml, SAML files, or restart Tomcat again yet.

We already established Tomcat is successfully deploying both BOE.war and AdminTools.war. Now we need to identify what Tomcat records for this exact /BOE/CMC 500 request.



grep -nEi "ERROR|SEVERE|Exception|500" catalina.out catalina.2026-09-21.log | tail -100


Yes — this confirms idp-meta-downloaded.xml is missing from BOE/WEB-INF.

Don’t create an empty file. First we need to see why SAML is configured and whether this UAT server is supposed to use it.

You are already in:

/data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/BOE/WEB-INF

Step 1 — run this

grep -n "idp-meta-downloaded.xml" securityContext.xml

Then run:

grep -Rni "saml.enabled\|FilesystemMetadataProvider\|idp-meta-downloaded.xml" . | head -100

Send me the output/screenshot of both.

Don’t restart Tomcat or change anything yet.


Yes — the latest screenshots show something important: the original AdminTools XML parse error is no longer the main problem. The logs now show AdminTools.war being deployed and finishing deployment, while the prominent error is:

Metadata file '/WEB-INF/idp-meta-downloaded.xml' does not exist

That is a SAML/IdP metadata configuration issue in the BOE application, separate from the AdminTools web.xml problem. SAP’s documentation says that when SAML is configured, the IdP metadata file must be placed in BOE/WEB-INF with exactly that filename; on non-Windows systems SAP also documents a path-separator adjustment in securityContext.xml. 

Don’t edit anything yet

First, let’s confirm whether the file is actually missing.

Run:

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/BOE/WEB-INF

Then:

ls -l idp-meta-downloaded.xml

Then:

grep -n "idp-meta-downloaded.xml" securityContext.xml

What I’m expecting

If the first command says:

No such file or directory

then we have confirmed why BOE is throwing that error.

Do not create an empty idp-meta-downloaded.xml file. It needs to contain the actual IdP metadata if SAML is intentionally enabled. SAP specifically instructs that the metadata be obtained from the IdP and copied into BOE/WEB-INF under that filename. 

Send me the output of those two commands. Then we can determine whether this UAT environment is supposed to have SAML enabled and what the correct next step is.




Yes — the second start was aborted because Tomcat is already running. This is not a new failure.

Your screen clearly says:

Existing PID file found during start.
Tomcat appears to still be running with PID 2489470.
Start aborted.

So DO NOT delete the PID file and DO NOT run start again. PID 2489470 is currently a Java/Tomcat process.

Now check the actual application status

Run:

ps -fp 2489470

Then:

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat/logs

And run:

grep -i -A5 -B5 "AdminTools\|Parse error\|dispatcher\|filter-mapping\|startup failed" catalina.out | tail -80

Important

We need this output because there are two separate things:

1. Tomcat process → appears to be running already. ✅
2. AdminTools deployment → we need to confirm whether our fixed WAR deployed successfully.

Don’t stop Tomcat again yet. Send me the output of those two commands.




Perfect. ✅ The verification is successful.

From your screenshot:

* dispatcher → no output ✅
* HSTS filter is before <servlet> ✅
* New WAR was created successfully: /tmp/AdminTools_fixed_20260921.war ✅

Now we can replace the original WAR.

1. First validate the WAR

Run:

unzip -t /tmp/AdminTools_fixed_20260921.war | tail -5

You should see:

No errors detected

2. Go to webapps

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps

3. Keep the original backup

You already have:

AdminTools.war.bak_20260921

So now replace the WAR:

cp -p /tmp/AdminTools_fixed_20260921.war AdminTools.war

4. Verify the actual AdminTools.war

Run:

unzip -p AdminTools.war WEB-INF/web.xml | grep -n "dispatcher"

It should return nothing.

Then:

unzip -p AdminTools.war WEB-INF/web.xml | grep -n -A6 -B4 "HstsFilter"

You should see the filter before the servlet section.

5. Start Tomcat

cd /data/tomcat_DH_TOMCAT_43_UAT/scripts
./tomcat.sh start

Wait for:

Tomcat started.

6. Check the NEW deployment log

Immediately run:

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat/logs

Then:

grep -i -A5 -B5 "AdminTools\|Parse error\|dispatcher\|filter-mapping\|startup failed" catalina.out | tail -80

Send me that output.

The key thing we’re looking for now is whether this old error is gone:

Element type "dispatcher" must be declared

If that disappears, we’ve successfully fixed the original web.xml deployment problem and can move on to whatever is causing the remaining CMC 500.







Yes — this is now correct. ✅

The screenshot shows the required order:

<filter>
<filter-mapping>
<servlet>
<servlet-mapping>

And the <dispatcher> line is gone.

Now we can safely rebuild the WAR.

1. Create the fixed WAR

Run:

cd /tmp/AdminTools_fix_20260921_v2

Then:

zip -qr /tmp/AdminTools_fixed_20260921.war .

2. Verify the new WAR

Run:

unzip -p /tmp/AdminTools_fixed_20260921.war WEB-INF/web.xml | grep -n "dispatcher"

It should return nothing.

Then:

unzip -p /tmp/AdminTools_fixed_20260921.war WEB-INF/web.xml | grep -n -A6 -B4 "HstsFilter"

You should see:

<filter>
    <filter-name>HstsFilter</filter-name>
    <filter-class>com.crystaldecisions.examples.HstsFilter</filter-class>
</filter>
<filter-mapping>
    <filter-name>HstsFilter</filter-name>
    <url-pattern>/*</url-pattern>
</filter-mapping>

3. Don’t replace the original yet

Send me the output/screenshot of those two verification commands.

Once that is confirmed, we’ll replace:

/data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/AdminTools.war

while keeping your backup:

AdminTools.war.bak_20260921

Then we’ll restart Tomcat and check whether the dispatcher / filter-mapping parse error is gone.




Yes — this screenshot confirms it 100%.

Your temporary web.xml currently has:

21  <servlet>
...
30  <servlet-mapping>
...
35  <!-- Enabled HSTS Filter -->
36  <filter>
...
42  <filter-mapping>
...

So the HSTS filter is still below the servlet section. That’s why Tomcat’s Servlet 2.3 parser rejects it.

Let’s move it now.

1. Save the HSTS block

Run:

sed -n '35,45p' /tmp/AdminTools_fix_20260921_v2/WEB-INF/web.xml > /tmp/hsts_block_20260921

2. Remove the old HSTS block

sed -i '35,45d' /tmp/AdminTools_fix_20260921_v2/WEB-INF/web.xml

3. Insert it before the servlet section

The <!-- Servlet Mappings --> comment is at line 19, so run:

sed -i '19r /tmp/hsts_block_20260921' /tmp/AdminTools_fix_20260921_v2/WEB-INF/web.xml

4. Verify

nl -ba /tmp/AdminTools_fix_20260921_v2/WEB-INF/web.xml | sed -n '15,50p'

You should now see this order:

19  <!-- Servlet Mappings -->
20  <!-- Enabled HSTS Filter -->
21  <filter>
22      <filter-name>HstsFilter</filter-name>
23      <filter-class>com.crystaldecisions.examples.HstsFilter</filter-class>
24  </filter>
25  <filter-mapping>
26      <filter-name>HstsFilter</filter-name>
27      <url-pattern>/*</url-pattern>
28  </filter-mapping>
...
30/31  <servlet>
...

And importantly:

No <dispatcher> anywhere.

Run this too:

grep -n "dispatcher" /tmp/AdminTools_fix_20260921_v2/WEB-INF/web.xml

It should return nothing.

Stop there

Send me the screenshot of the nl -ba output. Once I confirm the order, we’ll repackage the WAR.







nl -ba /tmp/AdminTools_fix_20260921_v2/WEB-INF/web.xml | sed -n '15,50p'

Now we found the exact reason for the Permission denied.

The extracted WEB-INF directory has no permissions for the owner, which is why even grep cannot read web.xml. This is only the temporary extracted copy, so we can safely fix its permissions.

Do this now

You’re currently in webapps.

1. Give yourself access to the temporary copy

chmod -R u+rwX /tmp/AdminTools_fix_20260921_v2

2. Check the file

ls -l /tmp/AdminTools_fix_20260921_v2/WEB-INF/web.xml

3. Remove the dispatcher

sed -i '/<dispatcher>REQUEST<\/dispatcher>/d' /tmp/AdminTools_fix_20260921_v2/WEB-INF/web.xml

4. Confirm it is gone

grep -n "dispatcher" /tmp/AdminTools_fix_20260921_v2/WEB-INF/web.xml

It should show nothing.

5. Confirm the HSTS section

grep -n -A6 -B4 "HstsFilter" /tmp/AdminTools_fix_20260921_v2/WEB-INF/web.xml

It should look like:

<filter-mapping>
    <filter-name>HstsFilter</filter-name>
    <url-pattern>/*</url-pattern>
</filter-mapping>

Important

Do not touch AdminTools.war yet. We are still working on /tmp/AdminTools_fix_20260921_v2, so the original WAR and your backup remain untouched.

Send me a screenshot after commands 3–5. Then we’ll repackage it safely.



I can see what happened. The extraction worked, but the command that was supposed to edit the temporary web.xml was blocked with Permission denied.

So do not repackage anything yet.

Let’s first check the permissions/ownership of that temporary file.

Run exactly:

ls -ld /tmp/AdminTools_fix_20260921_v2
ls -ld /tmp/AdminTools_fix_20260921_v2/WEB-INF
ls -l /tmp/AdminTools_fix_20260921_v2/WEB-INF/web.xml

Then run:

grep -n "dispatcher" /tmp/AdminTools_fix_20260921_v2/WEB-INF/web.xml

Send me that output.

Why we’re checking this

The important thing is:

* AdminTools.war does contain <dispatcher>REQUEST</dispatcher> ✅
* We successfully extracted the WAR ✅
* The edit to the extracted copy didn’t happen because of permissions ❌
* Therefore we haven’t changed the original WAR yet — good, your backup is still safe.

Once I see the ownership/permissions, I’ll give you the safest way to modify the temporary copy without touching the original WAR.



I see the issue in the screenshot. Nothing is wrong with the WAR yet. The command failed because your server’s security tool blocked rm -rf. The popup says you are not authorized to execute rm.

So we will not use rm at all.

Also, your grep failed because /tmp/AdminTools_fix_20260921 was never successfully created/extracted.

Do this from your current prompt

1. Go to webapps

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps

2. Create a new temporary directory

Use a new name so we don’t need to delete anything:

mkdir /tmp/AdminTools_fix_20260921_v2

If it says the directory already exists, tell me — don’t delete it.

3. Extract the WAR

unzip -q AdminTools.war -d /tmp/AdminTools_fix_20260921_v2

4. Now remove <dispatcher> from the extracted copy

sed -i '/<dispatcher>REQUEST<\/dispatcher>/d' /tmp/AdminTools_fix_20260921_v2/WEB-INF/web.xml

5. Verify

Run:

grep -n "dispatcher" /tmp/AdminTools_fix_20260921_v2/WEB-INF/web.xml

It should produce no output.

Then:

grep -n -A6 -B4 "HstsFilter" /tmp/AdminTools_fix_20260921_v2/WEB-INF/web.xml

You should see:

<filter-mapping>
    <filter-name>HstsFilter</filter-name>
    <url-pattern>/*</url-pattern>
</filter-mapping>

Stop there.

Don’t repackage the WAR yet. Send me a screenshot of steps 4–5. Then I’ll give you the exact command to rebuild AdminTools.war while preserving the existing WAR structure.





Yes. Now we have confirmed the exact root cause.

Your screenshot shows that inside AdminTools.war itself the file contains:

<filter-mapping>
    <filter-name>HstsFilter</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
</filter-mapping>

So the changes we made to the extracted webapps/AdminTools/WEB-INF/web.xml were being overwritten/ignored when Tomcat deployed AdminTools.war.

We need to fix the WAR, not the extracted folder.

Do this exactly

1. Stop Tomcat

From your current webapps directory:

cd /data/tomcat_DH_TOMCAT_43_UAT/scripts
./tomcat.sh stop

Wait until you see:

Tomcat stopped.

2. Go to webapps

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps

3. Make a backup of the original WAR

cp -p AdminTools.war AdminTools.war.bak_20260921

Do not skip this.

4. Extract the WAR to a temporary directory

rm -rf /tmp/AdminTools_fix_20260921
mkdir /tmp/AdminTools_fix_20260921
unzip -q AdminTools.war -d /tmp/AdminTools_fix_20260921

5. Remove the invalid dispatcher

Run:

sed -i '/<dispatcher>REQUEST<\/dispatcher>/d' /tmp/AdminTools_fix_20260921/WEB-INF/web.xml

6. Verify it is gone

grep -n "dispatcher" /tmp/AdminTools_fix_20260921/WEB-INF/web.xml

This command should return nothing.

Then run:

grep -n -A6 -B4 "HstsFilter" /tmp/AdminTools_fix_20260921/WEB-INF/web.xml

You should see the HSTS mapping without <dispatcher>.

STOP HERE

Don’t repackage the WAR yet.

Send me the output/screenshot of these two commands:

grep -n "dispatcher" /tmp/AdminTools_fix_20260921/WEB-INF/web.xml

and

grep -n -A6 -B4 "HstsFilter" /tmp/AdminTools_fix_20260921/WEB-INF/web.xml

I’ll verify the modified WAR contents first, then I’ll give you the exact repackaging and restart commands.




Yes — now we have the real reason. The latest logs are very useful.

What the logs prove

Your latest catalina output still contains:

Parse error at line [45] column [21]
Element type "dispatcher" must be declared

and:

Context [/AdminTools] startup failed due to previous errors

So Tomcat is still reading a web.xml that contains <dispatcher>.

That means our previous edit to the exploded directory did not become the file Tomcat is ultimately deploying.

And your log explicitly shows:

Deploying web application archive
.../webapps/AdminTools.war

This is the important part.

Tomcat is deploying AdminTools.war. If that WAR contains the original WEB-INF/web.xml, changing only:

webapps/AdminTools/WEB-INF/web.xml

is not enough. Tomcat can redeploy/re-expand the WAR and bring the original descriptor back. Tomcat’s deployment documentation confirms that WAR deployment/redeployment can recreate the exploded application directory. 

Don’t edit anything else yet

We need to confirm what is actually inside the WAR.

Go to:

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps

Then run this one command:

unzip -p AdminTools.war WEB-INF/web.xml | grep -n -A8 -B5 "HstsFilter"

Also run:

unzip -p AdminTools.war WEB-INF/web.xml | grep -n "dispatcher"

What I expect

If the second command returns something like:

<dispatcher>REQUEST</dispatcher>

then we’ve confirmed it:

The bad <dispatcher> is inside AdminTools.war, not just the exploded directory.

Don’t modify the WAR yet.

Send me the output of those two commands, and I’ll give you the exact next commands to fix the actual deployed file.

One more thing: your other screenshot shows:

Metadata file '/WEB-INF/idp-meta-downloaded.xml' does not exist

That’s a separate BOE/metadata issue appearing during application startup. Let’s fix the AdminTools.war deployment error first; otherwise we can’t reliably diagnose the CMC 500.

Yes — the fact that you still see HTTP 500 does not mean the filter change was necessarily wrong. The important point is that the browser screenshot only shows the final symptom. We need to see what Tomcat is reporting after the latest restart.

Also, I checked SAP documentation: AdminTools is part of the SAP BusinessObjects web applications used by CMC, so an AdminTools deployment failure can affect CMC access. 

Don’t change web.xml again yet

Run these exact commands.

1. Go to Tomcat logs

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat/logs

2. Find the latest AdminTools error

grep -Rni "AdminTools\|Parse error\|Context.*AdminTools\|startup failed" . | tail -80

3. Also check the latest Catalina log

tail -100 catalina.out

Send me the output of those two commands

I specifically want to see whether it still says:

Parse error ... web.xml

or whether the XML error is gone and we’re now dealing with a different SAP/CMC error.

That’s important because the Servlet 2.3 deployment descriptor does require filter/filter-mapping before servlet/servlet-mapping, so the structural change we made is consistent with that specification. 

Don’t make another edit until we see the new log. The current browser 500 by itself isn’t enough to identify the next root cause.

Yes. This screenshot confirms the issue clearly.

Your current web.xml has the HSTS filter after the servlet and servlet-mapping:

servlet
servlet-mapping
filter          ← currently here
filter-mapping

For this Servlet 2.3 web.xml, the filter section needs to come before the servlet section.

Do this exactly

You are already in:

/data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/AdminTools/WEB-INF

1. Create one more backup

cp web.xml web.xml.before_filter_move

2. Save the HSTS block temporarily

sed -n '35,45p' web.xml > /tmp/hsts_block

3. Remove the HSTS block from its current location

sed -i '35,45d' web.xml

4. Insert it before the Servlet Mappings section

sed -i '19r /tmp/hsts_block' web.xml

5. Verify the result

Run:

nl -ba web.xml | sed -n '1,50p'

You should now see approximately:

19  <!-- Enabled HSTS Filter -->
20  <filter>
21      <filter-name>HstsFilter</filter-name>
22      <filter-class>com.crystaldecisions.examples.HstsFilter</filter-class>
23  </filter>
24  <filter-mapping>
25      <filter-name>HstsFilter</filter-name>
26      <url-pattern>/*</url-pattern>
27  </filter-mapping>
...
30  <!-- Servlet Mappings -->
31  <servlet>
32      <servlet-name>logon</servlet-name>
...

Important: There should be no <dispatcher> in this HSTS mapping.

Then restart Tomcat

Go to the scripts directory:

cd /data/tomcat_DH_TOMCAT_43_UAT/scripts

Stop:

./tomcat.sh stop

Then start:

./tomcat.sh start

After it starts, check the latest AdminTools error:

grep -i -A5 -B5 "AdminTools\|Parse error\|Context.*AdminTools" /data/tomcat_DH_TOMCAT_43_UAT/tomcat/logs/catalina.out | tail -50

Don’t make any more changes yet. First show me the output of step 5 (nl -ba ...) before restarting if you want me to verify the XML order.






Yes — now I can see the actual problem.

Your <filter> / <filter-mapping> is after the <servlet> and <servlet-mapping> sections. With the Servlet 2.3 DTD used by this web.xml, the required order is:

filter → filter-mapping → servlet → servlet-mapping. 

Your current file is effectively:

servlet
servlet-mapping
filter
filter-mapping   ← wrong position

So let’s fix the order, not change the filter again.

1. First keep your backup

You already created:

web.xml.bak_20260921

Good.

2. Show me lines 1–50 before we edit

Run exactly:

nl -ba web.xml | sed -n '1,50p'

Send me the screenshot/output.

Don’t edit anything yet. I want to make sure the exact <servlet> starting line is what I expect before giving you the move command.







I checked the latest screenshots carefully. The restart worked, but AdminTools is still failing to deploy. The error has actually moved forward.

The important new error is:

Parse error at line [45] column [22]
The content of element type "filter-mapping" must match
"(filter-name,(url-pattern | servlet-name))"

So don’t make another random change. We need to inspect every filter-mapping in the file.

Do this now

You should still be in the scripts directory. Run:

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/AdminTools/WEB-INF

Then:

grep -n -A6 -B2 "<filter-mapping>" web.xml

This will show every filter-mapping and its contents.

Also run:

grep -nE "filter-mapping|filter-name|url-pattern|servlet-name|dispatcher" web.xml

Send me the output of those two commands.

Do not edit the file yet.

The first <filter-mapping> around line 42–45 looks correct visually, so I want to see all mappings. There may be another malformed mapping later in the file, and Tomcat is reporting the first one it encounters.

Also, your earlier <dispatcher> error is consistent with the Servlet 2.3 DTD: that DTD defines filter-mapping as filter-name followed by either url-pattern or servlet-name; it does not define dispatcher. 

So we’re now debugging the remaining XML structure, not the server restart.






Now we have the exact cause.

Your web.xml starts with:

<!DOCTYPE web-app PUBLIC
  "-//Sun Microsystems, Inc.//DTD Web Application 2.3//EN"
  "http://java.sun.com/dtd/web-app_2_3.dtd">

But later it contains:

<dispatcher>REQUEST</dispatcher>

The Servlet 2.3 DTD does not define <dispatcher>, which is why Tomcat reports:

Element type "dispatcher" must be declared

Do this exactly

You already created the backup:

web.xml.bak_20260921

So we’re safe.

1. Check how many dispatcher entries exist

You’re already in:

/data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/AdminTools/WEB-INF

Run:

grep -n "dispatcher" web.xml

You should see something like:

45: <dispatcher>REQUEST</dispatcher>

2. Remove the dispatcher line

If it shows only the REQUEST line, run:

sed -i '/<dispatcher>REQUEST<\/dispatcher>/d' web.xml

This is appropriate here because REQUEST is the default dispatcher, so removing that explicit element doesn’t change the normal request behavior.

3. Verify it was removed

grep -n "dispatcher" web.xml

If nothing is returned, that’s expected.

4. Check the relevant section

nl -ba web.xml | sed -n '35,50p'

You should see the filter-mapping without the dispatcher line.

Then Tomcat must reload AdminTools

Don’t restart yet if this is a shared UAT server without your senior’s approval. If you have permission to restart Tomcat, we can do that next.

If your senior has asked you specifically to fix and restart it, tell me “I have permission to restart”, and I’ll give you the exact stop/start commands for this Tomcat instance.

If you need to explain the fix to your senior:

Root cause: AdminTools/WEB-INF/web.xml declares Servlet 2.3 DTD, but contains the Servlet 2.4+ <dispatcher> element. Tomcat therefore fails XML validation and AdminTools doesn’t deploy, resulting in the CMC HTTP 500. The fix is to remove the unnecessary <dispatcher>REQUEST</dispatcher> entry (after backup), since REQUEST is the default.







Yes. Based on the error we can give you a specific procedure. The key is that Tomcat is rejecting AdminTools/WEB-INF/web.xml at the <dispatcher> element.

Do not remove <dispatcher> yet. First confirm the XML declaration/DOCTYPE, because that determines the correct fix.

Step 1 — Go to the file

You are currently in logs. Run:

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/AdminTools/WEB-INF

Step 2 — Take a backup

cp web.xml web.xml.bak_20260921

Step 3 — Show the beginning of the file

Run:

head -35 web.xml

Send me the output.

I specifically need to see the first few lines containing something like:

<!DOCTYPE web-app ...>

or

<web-app ... version="...">

Why this is important

Your error:

Element type "dispatcher" must be declared

is happening because the XML definition being used by web.xml does not recognize <dispatcher>.

If the file is using an old Servlet DTD/schema, while the application configuration contains:

<dispatcher>REQUEST</dispatcher>

Tomcat will reject the entire AdminTools application.

So the exact fix may be to correct the web.xml schema/DTD, rather than deleting the dispatcher.

Don’t restart Tomcat yet

Run only:

head -35 web.xml

and send me that output. Once I see those first lines, I’ll give you the exact edit command and restart/redeployment commands step-by-step.
The SAP BusinessObjects **AdminTools web application is failing to deploy in Tomcat because its WEB-INF/web.xml is failing XML/schema validation. Tomcat reports errors around the <dispatcher> element inside the filter-mapping section (Element type "dispatcher" must be declared) and subsequently marks the application unavailable. Because AdminTools does not start successfully, the CMC login request is returning HTTP 500 Internal Server Error.
 




Good, this screenshot is much clearer.

I can see around lines 43–46:

<filter-mapping>
    <filter-name>BasicFilter</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
</filter-mapping>

And Tomcat is complaining:

Element type "dispatcher" must be declared

This usually happens when:

* The web.xml version/schema does not support <dispatcher>, or
* The file is malformed/corrupted, causing XML validation to fail.

Before changing anything, let’s identify the Tomcat version.

Run:

cd /data/tomcat_DH_TOMCAT_43_UAT/tomcat
bin/version.sh

or if that doesn’t exist:

java -version

Send the output.

Also check whether this file was recently modified:

ls -l /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/AdminTools/WEB-INF/web.xml

From the logs, the issue is now narrowed down to AdminTools/WEB-INF/web.xml, not SAPCryptoLibs or the login credentials themselves. The next step is to confirm the Tomcat version before touching the XML.




nl -ba /data/tomcat_DH_TOMCAT_43_UAT/tomcat/webapps/AdminTools/WEB-INF/web.xml | sed -n '35,75p'



Yes, I can see the new log output. 👍

There are several log messages appearing after your login attempt, but the photo is too blurry to reliably read the exact exception.

Let’s extract only the important error lines instead of looking through the whole log.

1. Stop the live log

Press:

Ctrl+C

2. Run this command

grep -iE "error|exception|severe|failed" catalina.2026-09-21.log | tail -50

This will show the last 50 error/exception lines from today’s Tomcat log.

If that doesn’t show anything useful, run:

grep -iE "error|exception|severe|failed" catalina.out | tail -50

Send me a screenshot of the output. Don’t modify or restart anything yet—we’re just identifying the cause of the HTTP 500.



So check carefully in the ER data list where I have given, like as an ER and admin both have similar roles. But ER has an ability to, like exit an employee, like confirmation from the ER. So you can check the text file and you can analyze and you can know that. The problem is there is one case, actually ER can see all the cases, like in the put details, So now here can see all the details, like whatever in the PIP details list, they can see all the details. Okay, but there's one case is missing, like name, like some employee name, some employee name called Nizanthan, and his UID starts with, like some F two nine two nine zero. It is his UID, but the one case, he's just a normal employee. He's not a manager at all. So all other cases, similar cases are visible, similar to that employee who is a normal employee and who has been praised on him, the cases are visible for ER. But this case alone is not visible for ER. What may be the possible drawback, or like what may be the possible thing that ER not able to see this particular case alone? Because, like, this is only happening for one case. I don't know why it is happening. I analyzed end to end, like in the PIP details. The details are present. The PIP has been raised on him, and it is decided as an exit thing. So, however an ER should see in all the cases, like all the most of the status ERs have been able to see what are the status and what is the thing. Only in the exit case he can able to choose, like exit, like or any other thing like that. He can exit or extend. So in this case already the particular ER has exited certain cases and it all worked. But for particular this case alone it is not working. I don't know why. Unless possible, like possible defects or some bugs in the code. And tell me what may be the possible cause. Same for the admin also. Admin also from admin point of view also this case is not visible. Some certain cases, like one or two cases, not visible, but most of the cases are visible.






From your screenshots, the gap is specifically between “Check In Notes” and “Action” columns.

I can already see you’re using:

.session-table .checkin-notes-col,
.session-table td.checkin-notes-col {
    width: 80px;
    padding-left: 0.1rem !important;
    padding-right: 0.1rem !important;
}
.session-table .action-col,
.session-table td.action-col {
    width: 40px;
    padding-left: 0.1rem !important;
    padding-right: 0.1rem !important;
}

The problem is usually table-layout:auto or a min-width on one of the columns.

Quick Fix #1 (Most likely)

Add:

.session-table {
    table-layout: fixed;
    width: 100%;
}

Quick Fix #2

Force both columns to shrink:

.session-table th.checkin-notes-col,
.session-table td.checkin-notes-col {
    width: 60px !important;
    max-width: 60px !important;
    min-width: 60px !important;
    padding: 0 2px !important;
}
.session-table th.action-col,
.session-table td.action-col {
    width: 40px !important;
    max-width: 40px !important;
    min-width: 40px !important;
    padding: 0 2px !important;
}

Quick Fix #3

Inspect the column before “Action”. If it contains:

white-space: nowrap;

or

display:flex;
justify-content: space-between;

remove them temporarily and check.

Quick Check in Browser

Press F12 → Inspect the empty green area → Look at the highlighted <th> width.

If the <th class="checkin-notes-col"> is showing something like 150px+, then another CSS rule is overriding your width.

Search in VS Code:

checkin-notes-col
min-width
table-layout
th

and look for any:

min-width: 120px;
flex: 1;
width: auto;

that’s overriding your new CSS.

The safest fix is usually:

.session-table {
    table-layout: fixed;
}

followed by

.checkin-notes-col { width: 60px !important; }
.action-col { width: 40px !important; }

Can you send the HTML for those two <th> columns (Check In Notes and Action)? I can tell the exact line causing the gap.









If I understand correctly, you want the Edit Session option to be available only within a 5-day window centered around the session date:




Session Date	Editable?
2 days before	✅ Yes
1 day before	✅ Yes
Session day	✅ Yes
1 day after	✅ Yes
2 days after	✅ Yes
More than 2 days before	❌ No
More than 2 days after	❌ No

For example, if the session date is 5 June:

* 3 June → ✅ Edit allowed
* 4 June → ✅ Edit allowed
* 5 June → ✅ Edit allowed
* 6 June → ✅ Edit allowed
* 7 June → ✅ Edit allowed
* 2 June or earlier → ❌ Not allowed
* 8 June or later → ❌ Not allowed

The logic would be:

DateTime today = DateTime.Today;
DateTime sessionDate = session.SessionDate.Date;
int daysDifference = (today - sessionDate).Days;
bool canEdit = daysDifference >= -2 && daysDifference <= 2;

Or more simply:

bool canEdit = Math.Abs((today - sessionDate).Days) <= 2;

This allows editing from 2 days before to 2 days after the session date, and blocks editing outside that range.

If you share the current C# code where the date check is implemented, I can show the minimal change needed instead of rewriting the whole logic.
